---
- name: Deploy DEV - Django via Docker
  hosts: all
  become: true

  vars:
    container_name: hack-n-tap-dev
    image_name: srmarinho/hack-n-tap:latest

    app_port: 8000
    container_port: 8000

    django_superuser_username: tap
    django_superuser_email: tap@tap.lhc
    django_superuser_password: tijolo22

  tasks:

    - name: Docker pull da imagem
      command: docker pull {{ image_name }}

    - name: Parar container antigo (se existir)
      command: docker stop {{ container_name }}
      ignore_errors: yes

    - name: Remover container antigo (se existir)
      command: docker rm {{ container_name }}
      ignore_errors: yes

    - name: Subir novo container Django
      command: >
        docker run -d
        --name {{ container_name }}
        -p {{ app_port }}:{{ container_port }}
        -e DJANGO_SUPERUSER_USERNAME={{ django_superuser_username }}
        -e DJANGO_SUPERUSER_EMAIL={{ django_superuser_email }}
        -e DJANGO_SUPERUSER_PASSWORD={{ django_superuser_password }}
        --restart unless-stopped
        {{ image_name }}
        bash -c "
          python manage.py migrate &&
          python manage.py createsuperuser --noinput || true &&
          python manage.py runserver 0.0.0.0:8000
        "
      register: docker_run_result

    - name: Aguardar container inicializar
      pause:
        seconds: 5

    - name: Mostrar últimos logs do container
      command: docker logs --tail 30 {{ container_name }}
      register: docker_logs
      changed_when: false

    - name: Exibir logs
      debug:
        msg: "{{ docker_logs.stdout_lines }}"

    - name: Descobrir IP da máquina
      command: hostname -I
      register: host_ip
      changed_when: false

    - name: Informações finais de acesso
      debug:
        msg:
          - "🚀 Deploy concluído com sucesso"
          - "🌐 App: http://{{ host_ip.stdout.split()[0] }}:{{ app_port }}"
          - "🔐 Admin: http://{{ host_ip.stdout.split()[0] }}:{{ app_port }}/admin"
          - "👤 Usuário: {{ django_superuser_username }}"
          - "🔑 Senha: {{ django_superuser_password }}"
