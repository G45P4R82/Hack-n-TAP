# Shared Dependencies
pyserial>=3.5

# Terminal Interface (Textual)
textual>=0.47.1
textual-dev>=1.2.0

# GUI Interface (Kivy) - Windows Specific
# Note: For non-Windows systems, install 'kivy' directly without the extra deps
kivy[base]>=2.3.0
kivy_deps.sdl2>=0.6.0; sys_platform == "win32"
kivy_deps.glew>=0.3.1; sys_platform == "win32"
kivy_deps.angle>=0.3.3; sys_platform == "win32"
